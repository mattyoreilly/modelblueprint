# Aggregate data for a one-way plot

Aggregate data for a one-way plot

## Usage

``` r
aggregate_one_way(
  dt,
  var,
  obs,
  expo_col,
  split,
  bins,
  type_agg,
  time_unit = NA_character_,
  verbose = FALSE
)
```

## Value

data.table with columns: x, split, , exposure
