% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mb_seq.R
\name{mb_layer}
\alias{mb_layer}
\title{Construct a single layer for use in an \code{\link[=mb_seq]{mb_seq()}}}
\usage{
mb_layer(blueprints, aggregate_fn = NULL, yhat_name = NULL)
}
\arguments{
\item{blueprints}{A list of \code{modelblueprint} objects. Every blueprint must
have \verb{@yhat_name} set.}

\item{aggregate_fn}{\verb{function(df) -> df}. Receives the dataset after all blueprint
predictions have been appended and returns it, optionally with additional
columns. Defaults to the identity function when \code{blueprints} has one
element; required when there are multiple blueprints.}

\item{yhat_name}{\verb{[character(1)]} The primary output column of this layer.
When \code{aggregate_fn} is \code{NULL}, defaults to the single blueprint's \verb{@yhat_name}.
When \code{aggregate_fn} is provided, set this to the column \code{aggregate_fn} adds that
represents the layer's combined prediction.}
}
\value{
An \code{mb_layer} object.
}
\description{
A layer holds one or more \code{modelblueprint} objects that run in parallel.
Each blueprint appends its prediction (under \verb{@yhat_name}) to the dataset.
\code{aggregate_fn} then receives the enriched dataset and can add further derived
columns. \code{yhat_name} declares the layer's primary output column -- this is
what \code{predict()} returns when \code{return_all = FALSE}, and what downstream
layers can reference as a feature.
}
\examples{
\dontrun{
# Single model -- aggregate_fn and yhat_name default to the blueprint's yhat_name
l1 <- mb_layer(list(mb_freq))

# Two models combined as a product
l2 <- mb_layer(
  blueprints = list(mb_freq, mb_sev),
  yhat_name  = "pred_pure",
  aggregate_fn     = function(df) {
    df[["pred_pure"]] <- df[["pred_freq"]] * df[["pred_sev"]]
    df
  }
)
}

}
\seealso{
\code{\link[=mb_seq]{mb_seq()}} to combine layers into a pipeline.
}
