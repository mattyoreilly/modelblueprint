% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sami.R
\name{sami}
\alias{sami}
\alias{sami.default}
\alias{sami.list}
\title{SAMI Double Lift Chart}
\usage{
sami(data, ...)

\method{sami}{default}(
  data,
  obs,
  pred,
  bins = 50L,
  exposure = "exposure",
  type_agg = c("equal_exposure", "equal_range"),
  recalib = FALSE,
  ret = c("plot", "data"),
  ...
)

\method{sami}{list}(
  data,
  set = c("train", "test", "holdout"),
  bins = 20L,
  type_agg = c("equal_exposure", "equal_range"),
  recalib = FALSE,
  pred_names = NA_character_,
  ret = c("plot", "data"),
  ...
)
}
\arguments{
\item{data}{A list of \code{modelblueprint} objects. Must have length
2 or more. All blueprints must share the same \code{y_name},
\code{expo_name}, and training data structure.}

\item{...}{Passed to the default method.}

\item{obs}{\verb{[character(1)]} Name of the observed target column.}

\item{pred}{\verb{[character]} Names of two or more competing prediction
columns.}

\item{bins}{\verb{[integer(1)]} Number of bins. Default \code{20L}.}

\item{exposure}{\verb{[character(1)]} Name of the exposure column.
Default \code{"exposure"}.}

\item{type_agg}{\verb{[character(1)]} \code{"equal_exposure"} or \code{"equal_range"}.}

\item{recalib}{\verb{[logical(1)]} Recalibrate predictions. Default \code{FALSE}.}

\item{ret}{\verb{[character(1)]} \code{"plot"} or \code{"data"}.}

\item{set}{\verb{[character(1)]} Which dataset to use from the first
blueprint: \code{"train"}, \code{"test"}, or \code{"holdout"}.
Default \code{"train"}.}

\item{pred_names}{\verb{[character]} Optional vector of names for prediction
columns. Length must match \code{length(data)}. When \code{NA},
names are derived from \code{model_display_name}.}
}
\value{
A named list of plotly objects or a data.table depending on \code{ret}.

A named list of plotly objects or a data.table.
}
\description{
SAMI stands for \strong{Score Analysis for Model Improvement} — an actuarial
diagnostic technique for comparing two competing models.
}
\details{
For each pair of competing predictions, bins the ratio of one prediction to
another and plots the observed mean alongside both model means per bin.
Useful for diagnosing where two models systematically disagree and which
model better tracks the observed response across segments of the data.
}
\examples{
\donttest{
df <- data.frame(
  obs      = rnorm(500, 100),
  pred1    = rnorm(500, 100),
  pred2    = rnorm(500, 105),
  exposure = rep(1, 500)
)
sami(df, obs = "obs", pred = c("pred1", "pred2"), bins = 10)
}
\donttest{
mb1 <- modelblueprint(model = lm(mpg ~ wt, mtcars), train = mtcars,
                       y_name = "mpg", model_display_name = "lm_wt")
mb2 <- modelblueprint(model = lm(mpg ~ hp, mtcars), train = mtcars,
                       y_name = "mpg", model_display_name = "lm_hp")
sami(list(mb1, mb2), set = "train", bins = 10)
}
}
