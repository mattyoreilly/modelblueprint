% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelblueprint.R
\name{one_way.modelblueprint}
\alias{one_way.modelblueprint}
\title{One-way analysis for a modelblueprint}
\usage{
\method{one_way}{modelblueprint}(
  data,
  var = NA,
  set = c("train", "test", "holdout"),
  predictions = FALSE,
  split = NA_character_,
  bins = 35L,
  type_agg = c("equal_exposure", "equal_range"),
  ret = c("plot", "data"),
  ...,
  precomputed_preds = NULL
)
}
\arguments{
\item{data}{A \code{modelblueprint}.}

\item{var}{\verb{[character(1)]} Feature to plot on the x-axis.}

\item{set}{\verb{[character(1)]} Which dataset to use: \code{"train"},
\code{"test"}, or \code{"holdout"}. Default \code{"train"}.}

\item{predictions}{\verb{[logical(1)]} If \code{TRUE}, adds in-sample model
predictions as a second line (lift chart mode).
Default \code{FALSE}.}

\item{split}{\verb{[character(1) | NA]} Optional split variable.}

\item{bins}{\verb{[integer(1)]} Number of bins. Default \code{35L}.}

\item{type_agg}{\verb{[character(1)]} \code{"equal_exposure"} or \code{"equal_range"}.}

\item{ret}{\verb{[character(1)]} \code{"plot"} or \code{"data"}. Default \code{"plot"}.}

\item{...}{Further arguments passed to \code{\link[=one_way]{one_way()}}.}

\item{precomputed_preds}{\verb{[numeric | NULL]} Optional vector of pre-computed
predictions (one per row of the requested \code{set}). Only used when
\code{predictions = TRUE}. When supplied, the internal
\code{predict.modelblueprint()} call is skipped.}
}
\value{
A plotly object or data.table depending on \code{ret}.
}
\description{
Calls \code{\link[=one_way]{one_way()}} using the modelblueprint's target, exposure, and data
slots. Optionally overlays the model's in-sample predictions to produce
a lift chart (pass \code{predictions = TRUE}).
}
