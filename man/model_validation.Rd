% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_validation.R
\name{model_validation}
\alias{model_validation}
\title{Generate and save model validation plots}
\usage{
model_validation(
  mb,
  sets = c("train", "test", "holdout"),
  plots = c("validation", "oneway", "pdp", "stability", "shap"),
  validation_bins = 10L,
  one_way_bins = 10L,
  pdp_bins = 10L,
  split = NA_character_,
  filepath = getwd(),
  selfcontained = TRUE,
  seed = 1L
)
}
\arguments{
\item{mb}{A \code{modelblueprint} object.}

\item{sets}{\verb{[character]} Dataset splits to process.
Default \code{c("train", "test", "holdout")}. NULL splits are silently skipped.}

\item{plots}{\verb{[character]} Plot types to produce. Any combination
of \code{"validation"}, \code{"oneway"}, \code{"pdp"}, \code{"stability"}, \code{"shap"}.}

\item{validation_bins}{\verb{[integer(1)]} Bins for gain / pred-vs-obs charts.
Default \code{10L}.}

\item{one_way_bins}{\verb{[integer(1)]} Bins for one-way charts.
Default \code{10L}.}

\item{pdp_bins}{\verb{[integer(1)]} Bins for PDP charts. Default \code{10L}.}

\item{split}{\verb{[character(1)]} Column name to segment one-way
plots by. \code{NA} (default) produces unsplit charts.}

\item{filepath}{\verb{[character(1)]} Parent directory for all output.
Defaults to \code{getwd()}. A subdirectory named \verb{@model_display_name} is
created inside.}

\item{selfcontained}{\verb{[logical(1)]} Passed to \code{\link[=save_plots]{save_plots()}}. \code{TRUE}
(default) embeds all dependencies into each HTML file. Set to \code{FALSE}
for faster saves during development.}

\item{seed}{\verb{[integer(1)]} Seed for the random 50/50 split used
by the \code{"stability"} plots, applied via \code{\link[withr:with_seed]{withr::with_seed()}} so the result
is reproducible without disturbing the global RNG stream. Default \code{1L}.}
}
\value{
The root output directory path, invisibly.
}
\description{
Runs a configurable set of diagnostic and feature-analysis plots for each
requested dataset split and writes the results to structured HTML files
inside a directory named after \verb{@model_display_name}.
}
\details{
\strong{Output layout}

\if{html}{\out{<div class="sourceCode">}}\preformatted{<filepath>/
  <model_display_name>/
    <name>.tar.gz
    validation/
      <name>_<set>_validation_plots.html
    oneway/
      <name>_<set>_oneway_plots.html
      <name>_<set>_stability_plots.html   # if "stability" requested
    pdp/
      <name>_<set>_pdp_plots.html
    shap/
      <name>_<set>_shap_plots.html
}\if{html}{\out{</div>}}

\strong{Validation} plots include a gain chart, predicted-vs-observed calibration
chart, and grouped residuals — one HTML file per split.

\strong{One-way} plots cover every feature in \verb{@x_original_inputs}. If \code{split}
is supplied the column is passed to \code{\link[=one_way]{one_way()}}; otherwise charts are
unsplit.

\strong{Stability} plots are one-way charts split by a random 50/50 variable
(\code{"A"} / \code{"B"}). If the two lines overlap closely the patterns are stable
and not driven by random noise.

\strong{PDP} plots cover every feature in \verb{@x_original_inputs}.

\strong{SHAP} plots use \code{\link[=shap]{shap()}} with \code{type = "importance"}. The modelblueprint
must have \verb{@x_original_inputs} set and the model must support kernel SHAP.

The modelblueprint itself is serialised via \code{\link[=savemb]{savemb()}} into the root
output directory.
}
\seealso{
\code{\link[=gain]{gain()}}, \code{\link[=pred_vs_obs]{pred_vs_obs()}}, \code{\link[=residuals_grouped]{residuals_grouped()}}, \code{\link[=one_way]{one_way()}},
\code{\link[=pdp]{pdp()}}, \code{\link[=shap]{shap()}}, \code{\link[=save_plots]{save_plots()}}, \code{\link[=savemb]{savemb()}}
}
