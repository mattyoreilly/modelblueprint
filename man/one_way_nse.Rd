% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/one_way_nse.R
\name{one_way_nse}
\alias{one_way_nse}
\title{NSE wrapper for one_way}
\usage{
one_way_nse(
  data,
  var,
  obs = target,
  exposure = exposure,
  split = NULL,
  bins = 35L,
  type_agg = c("equal_exposure", "equal_range"),
  ret = c("plot", "data"),
  ...
)
}
\arguments{
\item{data}{A data frame or data.table.}

\item{var}{Bare column name for the x-axis variable.}

\item{obs}{Bare column name(s) to summarise. Wrap multiple in \code{c()},
e.g. \code{c(mpg, hp)}. Default \code{target}.}

\item{exposure}{Bare column name for exposure weights. Default \code{exposure}.}

\item{split}{Bare column name to split lines by. Omit or pass \code{NULL}
for no split.}

\item{bins}{\verb{[integer(1)]} Number of bins. Default 35.}

\item{type_agg}{\verb{[character(1)]} \code{"equal_exposure"} (default) or
\code{"equal_range"}.}

\item{ret}{\verb{[character(1)]} \code{"plot"} (default) or \code{"data"}.}

\item{...}{Additional arguments passed to \code{\link[=one_way]{one_way()}}.}
}
\value{
See \code{\link[=one_way]{one_way()}}.
}
\description{
Allows bare (unquoted) column names to be passed instead of strings.
Delegates to \code{\link[=one_way]{one_way()}} after resolving names.
}
\examples{
\dontrun{
one_way_nse(mtcars, wt, mpg, bins = 10)
one_way_nse(mtcars, cyl, c(mpg, hp), split = am)
one_way_nse(loans_df, loan_amount, c(default_rate, loss_rate),
            exposure = n_accounts, split = risk_band)
}

}
