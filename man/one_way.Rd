% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/one_way.R
\name{one_way}
\alias{one_way}
\alias{one_way.default}
\title{Create a one-way analysis plot}
\usage{
one_way(data, ...)

\method{one_way}{default}(
  data,
  var,
  obs = "target",
  exposure = "exposure",
  split = NA_character_,
  bins = 35L,
  time_unit = NA_character_,
  type_agg = c("equal_exposure", "equal_range"),
  ret = c("plot", "data"),
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table}.}

\item{...}{Arguments passed to methods.}

\item{var}{Column to plot on the x-axis. Bare name or string.}

\item{obs}{One or more columns to summarise on the y-axis (right
axis). Bare name, \code{c(a, b)}, or character vector.
Default \code{"target"}.}

\item{exposure}{Column of exposure weights. If the column does not exist
in \code{data}, every row is given weight 1. Bare name or
string. Default \code{"exposure"}.}

\item{split}{Optional column to split lines by. Bare name, string, or
\code{NA} / \code{NULL} for no split. Default \code{NA}.}

\item{bins}{\verb{[integer(1)]} Number of equal-exposure bins for numeric
variables with more than \code{bins} unique values. Default 35.}

\item{time_unit}{\verb{[character(1)]} For Date / POSIXct variables, the width
of each bin. Passed directly to \code{\link[base:cut.POSIXt]{base::cut.POSIXt()}}, so
any string it accepts works: \code{"month"}, \code{"week"},
\code{"2 weeks"}, \code{"12 hours"}, \code{"quarter"}, \code{"year"}, etc.
Ignored for non-date columns. Default \code{NA} (no date
binning — dates are shown as individual values).}

\item{type_agg}{\verb{[character(1)]} Binning strategy for numeric variables:
\code{"equal_exposure"} (default) or \code{"equal_range"}.}

\item{ret}{\verb{[character(1)]} \code{"plot"} (default) returns a plotly
object; \code{"data"} returns the aggregated data.table.}
}
\value{
A plotly object, or a data.table when \code{ret = "data"}, or \code{NULL}
with a warning if the plot cannot be produced.
}
\description{
Bins a feature variable, computes exposure-weighted means of one or more
observed variables per bin, and returns a dual-axis plotly chart (bars =
exposure, lines = weighted means). Optionally splits by a second variable.
}
\details{
Column name arguments (\code{var}, \code{obs}, \code{exposure}, \code{split}) accept both bare
(unquoted) names and strings, so \code{one_way(df, wt, mpg)} and
\code{one_way(df, "wt", "mpg")} are equivalent.
}
\examples{
\dontrun{
# bare names
one_way(mtcars, wt, mpg, bins = 10)
one_way(mtcars, cyl, c(mpg, hp), split = am)

# strings (equivalent)
one_way(mtcars, var = "wt", obs = "mpg", bins = 10)
one_way(mtcars, var = "cyl", obs = c("mpg", "hp"), split = "am")
}

}
