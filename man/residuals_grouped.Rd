% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residuals_grouped.R
\name{residuals_grouped}
\alias{residuals_grouped}
\alias{residuals_grouped.default}
\alias{residuals_grouped.modelblueprint}
\title{Grouped Residuals vs Predicted Plot}
\usage{
residuals_grouped(data, ...)

\method{residuals_grouped}{default}(
  data,
  pred = "predict",
  obs = "observed",
  exposure = "exposure",
  exposure_per_bin = 10,
  residual_type = c("raw", "pearson"),
  title = "",
  ret = c("plot", "data"),
  ...
)

\method{residuals_grouped}{modelblueprint}(
  data,
  set = c("train", "test", "holdout"),
  exposure_per_bin = 2500,
  residual_type = c("raw", "pearson"),
  title = NULL,
  ret = c("plot", "data"),
  ...,
  precomputed_preds = NULL
)
}
\arguments{
\item{data}{A \code{modelblueprint} object.}

\item{...}{Passed to \code{\link[=residuals_grouped.default]{residuals_grouped.default()}}.}

\item{pred}{\verb{[character(1)]} Name of the predictions column.}

\item{obs}{\verb{[character(1)]} Name of the observed target column.}

\item{exposure}{\verb{[character(1)]} Name of the exposure column.
Default \code{"exposure"}.}

\item{exposure_per_bin}{\verb{[numeric(1)]} Target exposure per bin.
Default \code{2500}. Automatically reduced if the
dataset is too small for meaningful grouping.}

\item{residual_type}{\verb{[character(1)]} \code{"raw"} or \code{"pearson"}.}

\item{title}{\verb{[character(1)]} Chart title. Defaults to
\code{model_display_name}.}

\item{ret}{\verb{[character(1)]} \code{"plot"} or \code{"data"}.}

\item{set}{\verb{[character(1)]} Dataset to use: \code{"train"},
\code{"test"}, or \code{"holdout"}. Default \code{"train"}.}

\item{precomputed_preds}{\verb{[numeric | NULL]} Optional vector of pre-computed
predictions (one per row of the requested \code{set}). When supplied, the
internal \code{predict.modelblueprint()} call is skipped.}
}
\value{
A plotly object or data.table depending on \code{ret}.

A plotly object or data.table depending on \code{ret}.
}
\description{
Bins predictions by exposure, computes grouped residuals, and overlays a
loess trend line with a 95\% confidence interval. Useful for diagnosing
systematic model bias across the prediction range.
}
\examples{
\dontrun{
df <- data.frame(
  obs      = rbinom(500, 1, 0.3),
  pred     = runif(500, 0.1, 0.5),
  exposure = rep(1, 500)
)
residuals_grouped(df, pred = "pred", obs = "obs", exposure = "exposure")
}
\dontrun{
mb <- modelblueprint(
  model  = glm(vs ~ wt + hp, data = mtcars, family = binomial),
  train  = mtcars,
  y_name = "vs",
  model_display_name = "logistic_vs"
)
residuals_grouped(mb)
}
}
