% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hosmer.R
\name{pred_vs_obs}
\alias{pred_vs_obs}
\alias{pred_vs_obs.default}
\alias{pred_vs_obs.modelblueprint}
\title{Predicted vs Observed Calibration Plot}
\usage{
pred_vs_obs(data, ...)

\method{pred_vs_obs}{default}(
  data,
  pred = "predict",
  obs = "observed",
  exposure = "exposure",
  bins = 10L,
  type_agg = c("equal_exposure", "equal_range"),
  title = "",
  ret = c("plot", "data"),
  ...
)

\method{pred_vs_obs}{modelblueprint}(
  data,
  set = c("train", "test", "holdout"),
  bins = 10L,
  type_agg = c("equal_exposure", "equal_range"),
  title = NULL,
  ret = c("plot", "data"),
  ...,
  precomputed_preds = NULL
)
}
\arguments{
\item{data}{A \code{modelblueprint} object.}

\item{...}{Passed to \code{\link[=pred_vs_obs.default]{pred_vs_obs.default()}}.}

\item{pred}{\verb{[character(1)]} Name of the predictions column.}

\item{obs}{\verb{[character(1)]} Name of the observed target column.}

\item{exposure}{\verb{[character(1)]} Name of the exposure column. If the column
is absent, every row is given weight 1.
Default \code{"exposure"}.}

\item{bins}{\verb{[integer(1)]} Number of bins. Default \code{10L}.}

\item{type_agg}{\verb{[character(1)]} \code{"equal_exposure"} or \code{"equal_range"}.}

\item{title}{\verb{[character(1)]} Chart title. Defaults to
\code{model_display_name} (with the set name appended when
plotting multiple sets).}

\item{ret}{\verb{[character(1)]} \code{"plot"} or \code{"data"}. Default \code{"plot"}.}

\item{set}{\verb{[character]} Dataset splits to use: any of \code{"train"},
\code{"test"}, \code{"holdout"}. Defaults to all available (non-NULL)
sets. When more than one set is used, a named list with
one result per set is returned.}

\item{precomputed_preds}{\verb{[numeric | NULL]} Optional vector of pre-computed
predictions (one per row of the requested \code{set}). When supplied, the
internal \code{predict.modelblueprint()} call is skipped.}
}
\value{
A plotly object or data.table depending on \code{ret}.

A plotly object or data.table depending on \code{ret}.
}
\description{
Creates a Hosmer-style calibration chart showing average predicted values
against average observed values across bins of the prediction space. A
yellow exposure bar on the secondary axis shows the distribution of data.
}
\examples{
\donttest{
mb <- modelblueprint(
  model  = glm(vs ~ wt + hp, data = mtcars, family = binomial),
  train  = mtcars,
  y_name = "vs",
  model_display_name = "logistic_vs"
)
pred_vs_obs(mb)
}
}
