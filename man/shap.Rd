% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shap.R
\name{shap}
\alias{shap}
\alias{shap.default}
\title{SHAP Feature Importance and Dependence Plots}
\usage{
shap(data, ...)

\method{shap}{default}(
  data,
  model,
  vars,
  exposure = "exposure",
  type = c("importance", "dependence"),
  nsim = 50L,
  sample_size = 500L,
  bins = 10L,
  type_agg = c("equal_exposure", "equal_range"),
  ret = c("plot", "data"),
  model_name = "model",
  pre_process_fun = function(df) df,
  feat_eng_fun = function(df) df,
  post_process_fun = function(preds, df_raw) preds,
  seed = 2024L,
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table}. Must contain the
columns named in \code{vars} and, optionally, \code{exposure}.}

\item{...}{Unused.}

\item{model}{A fitted model object.}

\item{vars}{\verb{[character]} Feature column names for which SHAP
values are computed.}

\item{exposure}{\verb{[character(1)]} Exposure weight column. If the
column is absent, every row is given weight 1.
Default \code{"exposure"}. Used only in the dependence
plot (exposure bars and weighted mean SHAP line).}

\item{type}{\verb{[character(1)]} Plot type: \code{"importance"} (default)
returns a signed horizontal bar chart of mean SHAP
per feature (purple = positive effect, blue =
negative effect, sorted by |SHAP| magnitude);
\code{"dependence"} returns a dual-axis chart (exposure
bars + mean SHAP per bin) for each variable in
\code{vars}.}

\item{nsim}{\verb{[integer(1)]} Number of random permutations per
observation. Higher values give more stable SHAP
estimates at the cost of compute time. Default
\code{50L}.}

\item{sample_size}{\verb{[integer(1)]} Rows sampled from \code{data} for SHAP
computation. Default \code{500L}. Seeded at 2024 for
reproducibility.}

\item{bins}{\verb{[integer(1)]} Number of bins for the dependence
plot x-axis. Default \code{10L}.}

\item{type_agg}{\verb{[character(1)]} Binning strategy for the
dependence plot: \code{"equal_exposure"} (default) or
\code{"equal_range"}.}

\item{ret}{\verb{[character(1)]} \code{"plot"} (default) or \code{"data"}.
\code{"data"} returns a data.table with one SHAP column
per feature in \code{vars}.}

\item{model_name}{\verb{[character(1)]} Label shown in plot titles.
Default \code{"model"}.}

\item{pre_process_fun}{\verb{function(df) -> df} applied before \code{feat_eng_fun}.
Default is the identity function.}

\item{feat_eng_fun}{\verb{function(df) -> df} (or matrix) that produces the
model input. Default is the identity function.}

\item{post_process_fun}{\verb{function(preds, df_raw) -> numeric} applied to raw
model predictions. Default is the identity function.}

\item{seed}{\verb{[integer(1)]} Seed for the row sample and the
internal SHAP permutations, applied via
\code{\link[withr:with_seed]{withr::with_seed()}} so the global RNG stream is left
undisturbed and results are reproducible. Default
\code{2024L}.}
}
\value{
A plotly object, a data.table (when \code{ret = "data"}), or a named
list of plotly objects when \code{type = "dependence"} and \code{vars} has more
than one element.
}
\description{
Computes approximate SHAP (SHapley Additive exPlanations) values using a
built-in permutation algorithm and returns either a \strong{feature importance}
chart (signed mean SHAP per feature  - purple = increases prediction, blue =
decreases prediction, sorted by magnitude) or a \strong{dependence} plot (mean
SHAP per bin alongside exposure bars). Both plots use the same dual-axis
Plotly style as \code{\link[=one_way]{one_way()}} and \code{\link[=pdp]{pdp()}}.
}
\details{
The algorithm is model-agnostic: it works with any model that has a
\code{predict()} method, including GLMs, XGBoost, randomForest, and H2O models.
No external packages beyond the modelblueprint dependencies are required.
}
\examples{
\donttest{
m <- lm(mpg ~ wt + hp + cyl + am, data = mtcars)

# Feature importance (mean |SHAP|)
shap(mtcars, model = m, vars = c("wt", "hp", "cyl", "am"),
     type = "importance", nsim = 10L, sample_size = 32L)

# Dependence plot for one feature
shap(mtcars, model = m, vars = "wt",
     type = "dependence", nsim = 10L, sample_size = 32L)
}

}
\seealso{
\code{\link[=one_way]{one_way()}}, \code{\link[=pdp]{pdp()}}
}
