% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shap.R
\name{compute_shap}
\alias{compute_shap}
\title{Permutation-based SHAP value computation}
\usage{
compute_shap(
  X_full,
  model,
  vars,
  nsim,
  pre_process_fun,
  feat_eng_fun,
  post_process_fun
)
}
\arguments{
\item{X_full}{data.frame of rows to explain  - ALL columns the
model needs, not just \code{vars}.}

\item{model}{Fitted model object.}

\item{vars}{\verb{[character]} Names of the features whose SHAP
values are computed. Must be columns of \code{X_full}.}

\item{nsim}{\verb{[integer(1)]} Permutations per observation.}

\item{pre_process_fun}{Pre-processing pipeline function.}

\item{feat_eng_fun}{Feature-engineering pipeline function.}

\item{post_process_fun}{Post-processing pipeline function.}
}
\value{
data.frame with the same row count as \code{X_full} and one column per
element of \code{vars} containing the SHAP values.
}
\description{
For each feature \code{j} in \code{vars} and each observation \code{i}, runs \code{nsim} random
permutations of the \code{vars} feature ordering. In permutation \code{s}:
\itemize{
\item \code{vars} features appearing \emph{before} \code{j} (the "coalition") keep actual
values; \code{vars} features at \code{j} and after are swapped to a background
row for \code{without_j}, while \code{with_j} keeps \code{j} actual but swaps
features after \code{j}.
\item Non-\code{vars} columns always retain their actual observation values so the
model can find every column it was trained on.
\item The marginal contribution is pred(with_j) - pred(without_j).
Averaging over permutations gives an unbiased Shapley value estimate.
}
}
\details{
All \code{2 * n * nsim} rows for a single feature are stacked into one
data.frame and passed to \code{\link[=model_predict]{model_predict()}} in a single call, following the
same batch-predict pattern as \code{\link[=compute_pdp]{compute_pdp()}}.
}
\keyword{internal}
