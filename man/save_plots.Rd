% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{save_plots}
\alias{save_plots}
\title{Save plots or HTML widgets to a single HTML file}
\usage{
save_plots(plots, file, selfcontained = TRUE, libdir = NULL)
}
\arguments{
\item{plots}{What to save. One of:
\itemize{
\item a single \code{htmlwidget} (e.g. a \code{plotly} object) or HTML tag,
\item a list of \code{htmlwidget} objects / tags,
\item an existing \code{shiny.tag.list}.
}

A single object is wrapped into a length-1 list; anything that is not
already a \code{shiny.tag.list} is coerced via \code{\link[htmltools:tagList]{htmltools::tagList()}}.}

\item{file}{\verb{[character(1)]} Path to the output HTML file.}

\item{selfcontained}{\verb{[logical(1)]} Embed all dependencies into a single
self-contained file? Requires Pandoc. Default \code{TRUE}.}

\item{libdir}{\verb{[character(1) | NULL]} Directory for the HTML dependency
files. When \code{NULL}, it is derived from \code{file} by appending \code{"_files"}
(keeping \code{file}'s directory). Removed after embedding when
\code{selfcontained = TRUE}.}
}
\value{
The path to the generated HTML file, invisibly.
}
\description{
Saves a plotly/htmlwidget object, a list of them, or an HTML tag list to an
HTML file. The output can optionally be made self-contained by embedding all
dependencies inline with Pandoc.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
p1 <- plotly::plot_ly(mtcars, x = ~mpg, y = ~wt)
p2 <- plotly::plot_ly(mtcars, x = ~mpg, y = ~hp)

# a single widget ...
save_plots(p1, tempfile(fileext = ".html"), selfcontained = FALSE)
# ... or a list of them
save_plots(list(p1, p2), tempfile(fileext = ".html"), selfcontained = FALSE)
\dontshow{\}) # examplesIf}
}
