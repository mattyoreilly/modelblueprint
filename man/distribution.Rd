% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{distribution}
\alias{distribution}
\alias{distribution.default}
\title{Plot the distribution of the target variable}
\usage{
distribution(data, ...)

\method{distribution}{default}(
  data,
  var = "target",
  exposure = "exposure",
  split = NA_character_,
  bins = 35L,
  time_unit = NA_character_,
  type_agg = c("equal_exposure", "equal_range"),
  ret = c("plot", "data"),
  verbose = FALSE,
  ...
)
}
\arguments{
\item{data}{A data frame or data.table. Must contain all columns
referenced by other arguments.}

\item{...}{Arguments passed to methods.}

\item{var}{Target column whose distribution to plot. Bare name or
string. Default \code{"target"}.}

\item{exposure}{Column of exposure weights. If the column does not exist
in \code{data}, every row is given weight 1. Bare name or
string. Default \code{"exposure"}.}

\item{split}{Optional column to group bars by. Bare name, string, or
\code{NA} / \code{NULL} for no split. Default \code{NA}.}

\item{bins}{\verb{[integer(1)]} Number of equal-exposure bins for numeric
targets with more than \code{bins} unique values. Default 35.}

\item{time_unit}{\verb{[character(1)]} For Date / POSIXct targets, the width of
each bin (passed to \code{\link[base:cut.POSIXt]{base::cut.POSIXt()}}). Default \code{NA}.}

\item{type_agg}{\verb{[character(1)]} Binning strategy for numeric targets:
\code{"equal_exposure"} (default) or \code{"equal_range"}.}

\item{ret}{\verb{[character(1)]} \code{"plot"} (default) returns a plotly
object; \code{"data"} returns the aggregated data.table.}

\item{verbose}{\verb{[logical(1)]} Report how many NA values of \code{var} were
moved to the trailing \code{"NA"} category? Default \code{FALSE}.}
}
\value{
A plotly object, or a data.table when \code{ret = "data"}, or \code{NULL}
with a warning if the plot cannot be produced.
}
\description{
Bins the target variable and plots the total exposure per bin as a bar
chart — an exposure-weighted distribution of the target. Shares its
argument conventions, binning, NA handling, split behaviour, and plotly
styling with \code{\link[=one_way]{one_way()}}; the only difference is that the target itself is
the x-axis variable, so there is no \code{obs} argument.
}
\details{
Column name arguments (\code{var}, \code{exposure}, \code{split}) accept both bare
(unquoted) names and strings, so \code{distribution(df, mpg)} and
\code{distribution(df, "mpg")} are equivalent.
}
\examples{
\donttest{
distribution(mtcars, mpg, bins = 10)
distribution(mtcars, var = "mpg", split = "am")
}

}
