% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shap.R
\name{plot_shap_importance}
\alias{plot_shap_importance}
\title{Render SHAP feature importance chart}
\usage{
plot_shap_importance(imp, model_name)
}
\arguments{
\item{imp}{data.frame with columns \code{feature}, \code{mean_shap}, and
\code{mean_abs_shap}.}

\item{model_name}{\verb{[character(1)]} Label shown in the chart title.}
}
\description{
Horizontal bar chart of \strong{mean SHAP} (signed) per feature, sorted by
magnitude (mean |SHAP|) so the most influential feature is at the top.
Purple bars indicate a feature whose average effect \strong{increases} the
prediction; blue bars indicate a feature that \strong{decreases} it.
A zero reference line and colour legend subtitle make the direction
immediately readable.
}
\details{
Styling matches \code{\link[=pdp]{pdp()}} and \code{\link[=one_way]{one_way()}}: transparent background, same
margins, \code{hovermode = "y unified"}.
}
\keyword{internal}
