% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdp.R
\name{pdp}
\alias{pdp}
\alias{pdp.default}
\title{Partial dependence plot for any predict()-compatible model}
\usage{
pdp(data, ...)

\method{pdp}{default}(
  data,
  var,
  obs,
  model,
  exposure = "exposure",
  bins = 10L,
  sample_size = 10000L,
  type_agg = c("equal_exposure", "equal_range"),
  model_name = "model",
  ret = c("plot", "data"),
  pre_process_fun = function(df) df,
  feat_eng_fun = function(df) df,
  post_process_fun = function(preds, df_raw) preds,
  seed = 2024L,
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table}.}

\item{...}{Arguments passed to methods.}

\item{var}{\verb{[character(1)]} Feature column to vary on the x-axis.}

\item{obs}{\verb{[character(1)]} Observed target column name.}

\item{model}{A fitted model object. Standard R models (lm, glm,
xgb, ranger, tidymodels workflows, etc.) and H2O models
are supported automatically - no extra arguments needed.}

\item{exposure}{\verb{[character(1)]} Exposure weight column. If absent,
every row is given weight 1. Default \code{"exposure"}.}

\item{bins}{\verb{[integer(1)]} Number of bins for numeric \code{var}.
Default 10.}

\item{sample_size}{\verb{[integer(1)]} Rows to sample for PDP computation.
Reducing this speeds up prediction at the cost of
accuracy. Default 10,000. The full dataset is always
used for the one-way actuals.}

\item{type_agg}{\verb{[character(1)]} Binning strategy: \code{"equal_exposure"}
(default) or \code{"equal_range"}.}

\item{model_name}{\verb{[character(1)]} Label shown in the plot legend.
Default \code{"model"}.}

\item{ret}{\verb{[character(1)]} \code{"plot"} (default) returns a plotly
object; \code{"data"} returns the aggregated data.table.}

\item{pre_process_fun}{\verb{function(df) -> df} applied to the data before
feature engineering. Default is the identity function.}

\item{feat_eng_fun}{\verb{function(df) -> df} (or matrix) applied after
pre-processing to produce the model input. Default is the identity
function.}

\item{post_process_fun}{\verb{function(preds, df_raw) -> numeric} applied to raw
model predictions. Default is the identity function.}

\item{seed}{\verb{[integer(1)]} Seed for the PDP row sample, applied via
\code{\link[withr:with_seed]{withr::with_seed()}} so the global RNG stream is left
undisturbed. Default \code{2024L}.}
}
\value{
A plotly object, or a data.table when \code{ret = "data"}, or \code{NULL}
with a warning when the variable cannot be plotted.
}
\description{
For each bin of \code{var}, the function fixes that feature at the bin midpoint
(numeric) or bin label (categorical), runs \code{predict()} across a sample of
the full dataset, and averages the predictions. The result shows the
marginal effect of \code{var} on model output, stripped of all correlations with
other features.
}
\details{
Alongside the PDP line the chart also shows:
\itemize{
\item Observed mean per bin (actual target, exposure-weighted)
\item Model average prediction per bin (in-sample, not PDP)
\item Global average observed and predicted reference lines
\item Yellow exposure bars (left axis) - identical style to \code{one_way()}
}
}
\examples{
\dontrun{
m <- lm(mpg ~ wt + hp + cyl, data = mtcars)

# Basic usage
pdp(mtcars, var = "wt", obs = "mpg", model = m)

# GLM - predict() is dispatched automatically
g <- glm(vs ~ wt + hp, data = mtcars, family = binomial)
pdp(mtcars, var = "wt", obs = "vs", model = g)

# Return aggregated data instead of a plot
pdp(mtcars, var = "wt", obs = "mpg", model = m, ret = "data")
}

}
\seealso{
\code{\link[=one_way]{one_way()}} for observed-only one-way analysis.
}
